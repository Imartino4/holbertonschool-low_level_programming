#include "main.h"

/**
 * _strlen - return de length of a string
 * @s: char
 *
 * Description -
 * Return: 0
 */
int _strlen(char *s) /*La entrada es lo que esta adentro de s(el string*/
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}
